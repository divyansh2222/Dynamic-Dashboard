/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['lh3.googleusercontent.com',"avatars.githubusercontent.com"], // List of hostnames for external images
  },
};

export default nextConfig;
